
package jprime.CNFApplication;

import java.util.HashMap;

public interface ICNFApplication extends jprime.gen.ICNFApplication {
	
	/**Add a pair of content id and its size to the host it belongs to.
	 * @param cid
	 * @param size
	 */
	public void addContentId(int cid, int size);
	
	/**
	 * @return
	 */
	public HashMap<Integer,Integer> getContentIds();
}
